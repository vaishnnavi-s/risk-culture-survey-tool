services:

  postgres:
    image: postgres:15
    container_name: riskculture-postgres
    restart: always
    environment:
      POSTGRES_DB: riskdb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: riskculture-redis
    ports:
      - "6379:6379"

volumes:
  postgres_data: